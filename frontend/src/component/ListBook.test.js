import { render, screen } from '@testing-library/react';
import ListBook from './ListBook';

test('renders list book component', () => {
  // Mock axios or just render
  render(<ListBook />);
  // Add assertions based on component
});