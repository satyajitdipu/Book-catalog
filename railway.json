{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd books && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "cd books && php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}